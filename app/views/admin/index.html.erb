<h1>Admin</h1>

<table>
  <thead>
    <thead>
      <tr>
        <th>Email</th><th>Role</th>
      </tr>
    </thead>
  </thead>
  <tbody>
  <% @users.each do |user| %>
    <tr>
      <td><%= user.email %></td>
      <td><%= user.roles.last.name %></td>
      <td>
        <%= form_tag("/admin/" + user.id.to_s + "?role=admin", method: :PUT) do %>
          <input type="submit" value="Make Admin"/>
        <% end %>
      </td>
    </tr>
  <% end %>
  </tbody>
</table>

<!-- <table>
    <thead>
        <tr>
            <th>User Email</th>
            <th>Make Admin</th>
            <th>Delete User</th>
        </tr>
    </thead>
    <tbody>
        <% @users.each do |user| %>
            <tr>
                <td><%= user.email %></td>
                <td>
                    <%= form_tag("/admin/" + user.id.to_s + "?role=admin", method: :PUT) do %>
                        <input type="submit" value="Make Admin"/>
                    <% end %>
                </td>
                <td><button type="submit" name="delete_user" value="Delete"></button></td>
            </tr>
        <% end %>
    </tbody>
</table> -->
